import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.urlshortener',
  appName: 'url-shortener',
  webDir: './dist/url-shortener-week-3/browser'
};

export default config;
